function handleImage(imageData) {
    const action = imageData.action;
    if (action === 'create') {
      // create image
    }
    else if (action == 'move') {
      // move image
    } else if (action == 'delete') {
      // delete image
    } else {
      // ..
    }


  }
