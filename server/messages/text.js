function handleText(textData) {
    const action = textData.action;
    if (action === 'create') {
      // create text
    }
    else if (action == 'move') {
      // move text
    } else if (action == 'edit') {
      // delete text
    } else if (action == 'delete') {
      // delete text
    } else {
      // ..
    }


  }