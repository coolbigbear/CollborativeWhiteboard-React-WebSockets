
    function draw(drawData) {
        const action = drawData.action;
        if (action === 'create') {
          // draw
        } else if (action == 'delete') {
          // erase
        } else {
          // ..
        }
  
  
      }