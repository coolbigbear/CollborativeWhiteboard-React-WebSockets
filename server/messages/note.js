function handleNote(noteData) {
    const action = noteData.action;
    if (action === 'create') {
      // create note
    }
    else if (action == 'move') {
      // move note
    } else if (action == 'edit') {
      // edit note
    } else if (action == 'delete') {
      // delete note
    } else {
      // ..
    }

  }